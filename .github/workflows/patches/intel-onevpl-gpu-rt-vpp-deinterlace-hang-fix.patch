diff --git a/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_alloc.cpp b/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_alloc.cpp
--- a/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_alloc.cpp	2026-04-23 02:53:36.640961102 +0900
+++ b/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_alloc.cpp	2026-04-19 23:32:27.555160554 +0900
@@ -75,7 +75,7 @@
 void Allocator::InitAlloc(const FeatureBlocks& /*blocks*/, TPushIA Push)
 {
     Push(BLK_Init
-        , [](StorageRW&, StorageRW& local) -> mfxStatus
+        , [](StorageRW& strg, StorageRW& local) -> mfxStatus
     {
         auto CreateAllocator = [](VideoCORE& core) -> IAllocation*
         {
@@ -83,6 +83,7 @@
         };
 
         local.Insert(Tmp::MakeAlloc::Key, new Tmp::MakeAlloc::TRef(CreateAllocator));
+        strg.Insert(Glob::MakeAlloc::Key, new Glob::MakeAlloc::TRef(CreateAllocator));
 
         return MFX_ERR_NONE;
     });
diff --git a/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_data.h b/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_data.h
--- a/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_data.h	2026-04-23 02:53:36.640961102 +0900
+++ b/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_data.h	2026-04-19 23:32:27.555160554 +0900
@@ -697,6 +697,7 @@
         mfxU16   LongTermIdx    = mfxU16(0);
         mfxU8    CodingType     = 0;
         Resource Raw;
+        Resource RawCopy;
         Resource RawTmp;
         Resource Rec;
         mfxFrameSurface1* pSurfIn = nullptr; //input surface, may be opaque
@@ -789,6 +790,7 @@
         mfxU32*             pBsDataLength       = nullptr;
         mfxBitstream*       pBsOut              = nullptr;
         mfxFrameSurface1*   pSurfReal           = nullptr;
+        mfxFrameSurface1    SurfInMetadata      = {};
         mfxEncodeCtrl       ctrl                = {};
         mfxU32              SkipCMD             = 0;
         Resource            BS;
@@ -809,6 +811,7 @@
         bool                bDontPatchBS        = false;
         bool                bRecode             = false;
         bool                bFreed              = false;
+        bool                bSurfInRefReleased  = false;
         bool                bForceLongStartCode = false;
         bool                bSaliencyEncEnabled = false;
         IntraRefreshState   IRState             = {};
@@ -1416,9 +1419,11 @@
         using CqmPPS              = StorageVar<__LINE__ - _KD, std::vector<Base::PPS>>;
         using SliceInfo           = StorageVar<__LINE__ - _KD, std::vector<Base::SliceInfo>>;
         using AllocRaw            = StorageVar<__LINE__ - _KD, IAllocation>;
+        using AllocRawCopy        = StorageVar<__LINE__ - _KD, IAllocation>;
         using AllocRawTmp         = StorageVar<__LINE__ - _KD, IAllocation>;
         using AllocOpq            = StorageVar<__LINE__ - _KD, IAllocation>;
         using AllocRec            = StorageVar<__LINE__ - _KD, IAllocation>;
+        using MakeAlloc           = StorageVar<__LINE__ - _KD, std::function<IAllocation*(VideoCORE&)>>;
         using AllocBS             = StorageVar<__LINE__ - _KD, IAllocation>;
         using AllocMBQP           = StorageVar<__LINE__ - _KD, IAllocation>;
         using MBQPAllocInfo       = StorageVar<__LINE__ - _KD, MBQPAllocFrameInfo>;
diff --git a/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_legacy.cpp b/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_legacy.cpp
--- a/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_legacy.cpp	2026-04-23 02:53:36.641961132 +0900
+++ b/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_legacy.cpp	2026-04-23 04:23:55.075866232 +0900
@@ -37,12 +37,53 @@
 #include <cmath>
 #include <iterator>
 #include "libmfx_core.h"
+#include "libmfx_allocator.h"
 #include "mfx_enc_common.h"
 
 
 using namespace HEVCEHW;
 using namespace HEVCEHW::Base;
 
+namespace
+{
+MFXIScheduler *GetSchedulerFromCore(VideoCORE &core)
+{
+    mfxSession session = core.GetSession();
+
+    if (session == nullptr)
+    {
+        return nullptr;
+    }
+
+    return session->m_pScheduler;
+}
+
+bool IsHighFrameRateVppDi(VideoCORE &core)
+{
+    return IsHighFrameRateVppDiScheduler(GetSchedulerFromCore(core));
+}
+
+bool ShouldDecoupleVideoMemoryInput(VideoCORE & /*core*/, const mfxFrameSurface1 *surface)
+{
+    bool is_vpp_di_output = false;
+
+    if (surface != nullptr && surface->Data.MemId != nullptr)
+    {
+        is_vpp_di_output = IsVppDiOutputMemId(surface->Data.MemId);
+    }
+
+    if (is_vpp_di_output == false
+        && surface != nullptr
+        && surface->FrameInterface != nullptr
+        && surface->FrameInterface->Context != nullptr)
+    {
+        is_vpp_di_output = static_cast<mfxFrameSurfaceBaseInterface *>(surface->FrameInterface->Context)->IsVppDiOutput();
+    }
+
+    return is_vpp_di_output;
+}
+}
+
 
 void Legacy::SetSupported(ParamSupport& blocks)
 {
@@ -921,6 +962,19 @@
         auto& caps = Glob::EncodeCaps::Get(strg);
         auto& defchain = Glob::Defaults::Get(strg);
         SetDefaults(par, Defaults::Param(par, caps, core.GetHWType(), defchain), core.IsExternalFrameAllocator());
+
+        // Once HEVC starts consuming the decoupled Raw surface, keeping the default async
+        // depth for VPP DI workloads creates an unnecessarily deep in-flight queue of Raw /
+        // RawCopy surfaces. Clamp normal DI and bob DI separately so the decoupled pools
+        // stay shallow enough to avoid ownership races without touching non-DI workloads.
+        if (IsHighFrameRateVppDi(core) && par.AsyncDepth > 1)
+        {
+            par.AsyncDepth = 1;
+        }
+        else if (IsVppDiScheduler(GetSchedulerFromCore(core)) && par.AsyncDepth > 4)
+        {
+            par.AsyncDepth = 4;
+        }
     });
 }
 
@@ -982,9 +1036,9 @@
         auto& par = Glob::VideoParam::Get(strg);
 
         auto pReorderer = make_storable<Reorderer>();

         pReorderer->BufferSize = par.mfx.GopRefDist - 1;
         pReorderer->MaxReorder = par.mfx.GopRefDist - 1;
         pReorderer->DPB        = &m_prevTask.DPB.After;
 
         pReorderer->Push(
@@ -1131,11 +1186,16 @@
         , [this](StorageRW& strg, StorageRW& local) -> mfxStatus
     {
         auto& par = Glob::VideoParam::Get(strg);
+        auto& core = Glob::VideoCore::Get(strg);
         mfxFrameAllocRequest raw = {};
+        bool is_vpp_di = IsVppDiScheduler(GetSchedulerFromCore(core));
+        bool is_high_frame_rate_vpp_di = IsHighFrameRateVppDi(core);
 
         raw.Info = par.mfx.FrameInfo;
         auto& rawInfo = Tmp::RawInfo::GetOrConstruct(local, raw);
-        SetDefault(rawInfo.NumFrameMin, GetMaxRaw(par));
+        SetDefault(
+            rawInfo.NumFrameMin,
+            is_vpp_di ? GetMaxRawDecoupled(par, is_high_frame_rate_vpp_di) : GetMaxRaw(par));
         SetDefault(rawInfo.Type
             , mfxU16(MFX_MEMTYPE_FROM_ENCODE
                 | MFX_MEMTYPE_DXVA2_DECODER_TARGET
@@ -1186,7 +1246,7 @@
         };
 
         bool isD3D9SimWithVideoMem = IsD3D9Simulation(Glob::VideoCore::Get(strg)) && (par.IOPattern & MFX_IOPATTERN_IN_VIDEO_MEMORY);
-        if (par.IOPattern == MFX_IOPATTERN_IN_SYSTEM_MEMORY || isD3D9SimWithVideoMem)
+        if (par.IOPattern == MFX_IOPATTERN_IN_SYSTEM_MEMORY || par.IOPattern == MFX_IOPATTERN_IN_VIDEO_MEMORY || isD3D9SimWithVideoMem)
         {
             sts = AllocRaw(rawInfo.NumFrameMin);
             MFX_CHECK_STS(sts);
@@ -1500,6 +1560,9 @@
         if (real.Contains(Glob::AllocRawTmp::Key))
             Glob::AllocRawTmp::Get(real).UnlockAll();
 
+        if (real.Contains(Glob::AllocRawCopy::Key))
+            Glob::AllocRawCopy::Get(real).UnlockAll();
+
         ResetState();
 
         return MFX_ERR_NONE;
@@ -1509,7 +1572,7 @@
 void Legacy::FrameSubmit(const FeatureBlocks& /*blocks*/, TPushFS Push)
 {
     Push(BLK_CheckSurf
-        , [](
+        , [this](
             const mfxEncodeCtrl* /*pCtrl*/
             , const mfxFrameSurface1* pSurf
             , mfxBitstream& /*bs*/
@@ -1519,6 +1582,29 @@
         MFX_CHECK(pSurf, MFX_ERR_NONE);
 
         auto& par = Glob::VideoParam::Get(global);
+        auto& core = Glob::VideoCore::Get(global);
+        bool shouldDecoupleVideoMemoryInput = ShouldDecoupleVideoMemoryInput(core, pSurf);
+
+        if (par.IOPattern == MFX_IOPATTERN_IN_VIDEO_MEMORY
+            && shouldDecoupleVideoMemoryInput
+            && !global.Contains(Glob::AllocRawCopy::Key))
+        {
+            MFX_CHECK(global.Contains(Glob::MakeAlloc::Key), MFX_ERR_UNDEFINED_BEHAVIOR);
+
+            std::unique_ptr<IAllocation> pAlloc(Glob::MakeAlloc::Get(global)(core));
+            mfxFrameAllocRequest req = {};
+            bool is_high_frame_rate_vpp_di = IsHighFrameRateVppDi(core);
+            req.Info = par.mfx.FrameInfo;
+            req.NumFrameMin = GetMaxRawCopy(par, is_high_frame_rate_vpp_di);
+            req.NumFrameSuggested = req.NumFrameMin;
+            req.Type = MFX_MEMTYPE_INTERNAL_FRAME | MFX_MEMTYPE_SYSTEM_MEMORY;
+
+            auto sts = pAlloc->Alloc(req, true);
+            MFX_CHECK_STS(sts);
+
+            global.Insert(Glob::AllocRawCopy::Key, std::move(pAlloc));
+        }
+
         MFX_CHECK(LumaIsNull(pSurf) == (pSurf->Data.UV == 0), MFX_ERR_UNDEFINED_BEHAVIOR);
         MFX_CHECK(pSurf->Info.Width >= par.mfx.FrameInfo.Width, MFX_ERR_INVALID_VIDEO_PARAM);
         MFX_CHECK(pSurf->Info.Height >= par.mfx.FrameInfo.Height, MFX_ERR_INVALID_VIDEO_PARAM);
@@ -1592,6 +1678,8 @@
             tpar.ctrl = *pCtrl;
         }
         tpar.pSurfReal = tpar.pSurfIn;
+        tpar.SurfInMetadata = {};
+        tpar.bSurfInRefReleased = false;
 
         core.IncreaseReference(*tpar.pSurfIn);
 
@@ -1658,7 +1746,11 @@
             task.RawTmp = Glob::AllocRawTmp::Get(global).Acquire();
             MFX_CHECK(task.RawTmp.Mid, MFX_ERR_UNDEFINED_BEHAVIOR);
         }
- 
+        if (global.Contains(Glob::AllocRawCopy::Key))
+        {
+            task.RawCopy = Glob::AllocRawCopy::Get(global).Acquire();
+            MFX_CHECK(task.RawCopy.Mid, MFX_ERR_UNDEFINED_BEHAVIOR);
+        }
         task.Rec = Glob::AllocRec::Get(global).Acquire();
         task.BS = Glob::AllocBS::Get(global).Acquire();
         MFX_CHECK(task.BS.Idx != IDX_INVALID, MFX_ERR_UNDEFINED_BEHAVIOR);
@@ -1818,15 +1910,26 @@
         auto& core = Glob::VideoCore::Get(global);
         auto& par = Glob::VideoParam::Get(global);
         auto& task = Task::Common::Get(s_task);
+        bool shouldDecoupleVideoMemoryInput = ShouldDecoupleVideoMemoryInput(core, task.pSurfReal);
 
         bool bInternalFrame =
             par.IOPattern == MFX_IOPATTERN_IN_SYSTEM_MEMORY ||
             (IsD3D9Simulation(Glob::VideoCore::Get(global)) && (par.IOPattern & MFX_IOPATTERN_IN_VIDEO_MEMORY))
+            || shouldDecoupleVideoMemoryInput
             || task.bSkip;
 
         mfxFrameSurface1* surface = task.pSurfReal;
         bool bIntAlloc = surface && (surface->Data.MemType & MFX_MEMTYPE_INTERNAL_FRAME);
 
+        // VEBOX DI output is copied to task.Raw.Mid in BLK_CopySysToRaw.
+        // Redirect the encoder input handle to task.Raw.Mid so BeginPicture()
+        // reads the decoupled surface instead of the original VPP output surface.
+        if (shouldDecoupleVideoMemoryInput)
+        {
+            MFX_CHECK(task.Raw.Mid, MFX_ERR_UNDEFINED_BEHAVIOR);
+            return core.GetFrameHDL(task.Raw.Mid, &task.HDLRaw.first);
+        }
+
         // Video memory internal allocation
         MFX_CHECK(!(bIntAlloc && par.IOPattern == MFX_IOPATTERN_IN_VIDEO_MEMORY),
             core.GetFrameHDL(*task.pSurfReal, task.HDLRaw));
@@ -1849,12 +1952,14 @@
         auto& par = Glob::VideoParam::Get(global);
         auto& task = Task::Common::Get(s_task);
         auto dflts = GetRTDefaults(global);
-
+        auto& core = Glob::VideoCore::Get(global);
+        bool shouldDecoupleVideoMemoryInput = ShouldDecoupleVideoMemoryInput(core, task.pSurfReal);
+        bool is_high_frame_rate_vpp_di = IsHighFrameRateVppDi(core);
         bool videoMemory =
             (par.IOPattern == MFX_IOPATTERN_IN_VIDEO_MEMORY
-                && !IsD3D9Simulation(Glob::VideoCore::Get(global)));
+                && !IsD3D9Simulation(core));
 
-        MFX_CHECK(!(task.bSkip || videoMemory), MFX_ERR_NONE);
+        MFX_CHECK(!(task.bSkip || (videoMemory && !shouldDecoupleVideoMemoryInput)), MFX_ERR_NONE);
 
         mfxFrameSurface1 surfSrc = MakeSurface(par.mfx.FrameInfo, *task.pSurfReal);
         mfxFrameSurface1 surfDst = MakeSurface(par.mfx.FrameInfo, task.Raw.Mid);
@@ -1866,7 +1971,150 @@
         mfxU16 inMemType = static_cast<mfxU16>((par.IOPattern & MFX_IOPATTERN_IN_SYSTEM_MEMORY ? MFX_MEMTYPE_SYSTEM_MEMORY : MFX_MEMTYPE_DXVA2_DECODER_TARGET) |
             MFX_MEMTYPE_EXTERNAL_FRAME);
 
-        return  dflts.base.RunFastCopyWrapper(
+        if (shouldDecoupleVideoMemoryInput)
+        {
+            mfxStatus sts = MFX_ERR_NONE;
+            if (is_high_frame_rate_vpp_di)
+            {
+                // Bob DI still shows a race if the decoupled chain starts with a GPU-side
+                // fast copy from the live VEBOX output. For bob only, bypass RawCopy and
+                // copy the completed VEBOX output directly into the encoder-owned Raw
+                // surface under CPU locks so encoder input ownership is severed in one step.
+                CommonCORE_VPL* ccore = dynamic_cast<CommonCORE_VPL*>(&core);
+                MFX_CHECK(ccore != nullptr, MFX_ERR_UNDEFINED_BEHAVIOR);
+
+                {
+                    mfxFrameSurface1_scoped_lock inLock(&surfSrc, ccore);
+                    MFX_SAFE_CALL(inLock.lock(MFX_MAP_READ));
+
+                    mfxFrameSurface1_scoped_lock outLock(&surfDst, ccore);
+                    MFX_SAFE_CALL(outLock.lock(MFX_MAP_WRITE));
+
+                    if (par.mfx.FrameInfo.FourCC == MFX_FOURCC_P010)
+                    {
+                        for (size_t y = 0; y < size_t(par.mfx.FrameInfo.Height); ++y)
+                        {
+                            std::copy(
+                                &surfSrc.Data.Y16[(surfSrc.Data.Pitch >> 1) * y],
+                                &surfSrc.Data.Y16[(surfSrc.Data.Pitch >> 1) * y + par.mfx.FrameInfo.Width],
+                                &surfDst.Data.Y16[(surfDst.Data.Pitch >> 1) * y]);
+                        }
+
+                        mfxU16* dst_uv = reinterpret_cast<mfxU16*>(surfDst.Data.UV);
+                        mfxU16* src_uv = reinterpret_cast<mfxU16*>(surfSrc.Data.UV);
+                        for (int y = 0; y < par.mfx.FrameInfo.Height / 2; ++y)
+                        {
+                            std::copy(
+                                &src_uv[(surfSrc.Data.Pitch >> 1) * y],
+                                &src_uv[(surfSrc.Data.Pitch >> 1) * y + par.mfx.FrameInfo.Width],
+                                &dst_uv[(surfDst.Data.Pitch >> 1) * y]);
+                        }
+                    }
+                    else
+                    {
+                        for (size_t y = 0; y < size_t(par.mfx.FrameInfo.Height); ++y)
+                        {
+                            std::copy(
+                                &surfSrc.Data.Y[surfSrc.Data.Pitch * y],
+                                &surfSrc.Data.Y[surfSrc.Data.Pitch * y + par.mfx.FrameInfo.Width],
+                                &surfDst.Data.Y[surfDst.Data.Pitch * y]);
+                        }
+
+                        for (int y = 0; y < par.mfx.FrameInfo.Height / 2; ++y)
+                        {
+                            std::copy(
+                                &surfSrc.Data.UV[surfSrc.Data.Pitch * y],
+                                &surfSrc.Data.UV[surfSrc.Data.Pitch * y + par.mfx.FrameInfo.Width],
+                                &surfDst.Data.UV[surfDst.Data.Pitch * y]);
+                        }
+                    }
+                }
+
+                sts = core.LockFrame(surfDst, MFX_MAP_READ);
+                MFX_CHECK_STS(sts);
+
+                sts = core.UnlockFrame(surfDst);
+                MFX_CHECK_STS(sts);
+
+                // Bob DI also needs to release the original VPP output reference once
+                // the encoder-owned Raw surface has been finalized. Otherwise later HEVC
+                // stages still keep the live VEBOX output surface referenced through
+                // task.pSurfIn even though BeginPicture() has already been redirected to
+                // task.Raw.Mid, which defeats part of the ownership split for bob.
+                if (task.bSurfInRefReleased == false
+                    && task.pSurfIn != nullptr
+                    && task.Raw.Mid)
+                {
+                    mfxFrameSurface1 *original_input_surface = task.pSurfIn;
+                    task.SurfInMetadata = MakeSurface(par.mfx.FrameInfo, task.Raw.Mid);
+                    task.SurfInMetadata.FrameInterface = nullptr;
+                    task.SurfInMetadata.Data.TimeStamp = original_input_surface->Data.TimeStamp;
+                    task.SurfInMetadata.Data.FrameOrder = original_input_surface->Data.FrameOrder;
+                    task.SurfInMetadata.Info.PicStruct = original_input_surface->Info.PicStruct;
+                    task.SurfInMetadata.Info.FrameId = original_input_surface->Info.FrameId;
+
+                    sts = core.DecreaseReference(*original_input_surface);
+                    MFX_CHECK_STS(sts);
+
+                    task.pSurfIn = &task.SurfInMetadata;
+                    task.bSurfInRefReleased = true;
+                }
+            }
+            else
+            {
+                MFX_CHECK(task.RawCopy.Mid, MFX_ERR_UNDEFINED_BEHAVIOR);
+
+                mfxFrameSurface1 surfMid = MakeSurface(par.mfx.FrameInfo, task.RawCopy.Mid);
+                surfMid.Info.Shift = surfDst.Info.Shift;
+
+                sts = dflts.base.RunFastCopyWrapper(
+                    surfMid,
+                    MFX_MEMTYPE_INTERNAL_FRAME | MFX_MEMTYPE_SYSTEM_MEMORY,
+                    surfSrc,
+                    inMemType);
+                MFX_CHECK_STS(sts);
+
+                // Once the original VPP output has been copied into the encoder-owned RawCopy
+                // staging surface, HEVC no longer needs to keep the VEBOX output alive. Release
+                // the original input reference immediately so VEBOX can recycle it while the
+                // encoder-owned Raw path is still being finalized.
+                if (task.bSurfInRefReleased == false
+                    && task.pSurfIn != nullptr
+                    && task.Raw.Mid)
+                {
+                    mfxFrameSurface1 *original_input_surface = task.pSurfIn;
+                    task.SurfInMetadata = MakeSurface(par.mfx.FrameInfo, task.Raw.Mid);
+                    task.SurfInMetadata.FrameInterface = nullptr;
+                    task.SurfInMetadata.Data.TimeStamp = original_input_surface->Data.TimeStamp;
+                    task.SurfInMetadata.Data.FrameOrder = original_input_surface->Data.FrameOrder;
+                    task.SurfInMetadata.Info.PicStruct = original_input_surface->Info.PicStruct;
+                    task.SurfInMetadata.Info.FrameId = original_input_surface->Info.FrameId;
+
+                    sts = core.DecreaseReference(*original_input_surface);
+                    MFX_CHECK_STS(sts);
+
+                    task.pSurfIn = &task.SurfInMetadata;
+                    task.bSurfInRefReleased = true;
+                }
+
+                sts = dflts.base.RunFastCopyWrapper(
+                    surfDst,
+                    MFX_MEMTYPE_INTERNAL_FRAME | MFX_MEMTYPE_DXVA2_DECODER_TARGET | MFX_MEMTYPE_FROM_ENCODE,
+                    surfMid,
+                    MFX_MEMTYPE_INTERNAL_FRAME | MFX_MEMTYPE_SYSTEM_MEMORY);
+                MFX_CHECK_STS(sts);
+
+                sts = core.LockFrame(surfDst, MFX_MAP_READ);
+                MFX_CHECK_STS(sts);
+
+                sts = core.UnlockFrame(surfDst);
+                MFX_CHECK_STS(sts);
+            }
+
+            return MFX_ERR_NONE;
+        }
+
+        return dflts.base.RunFastCopyWrapper(
             surfDst,
             MFX_MEMTYPE_INTERNAL_FRAME | MFX_MEMTYPE_DXVA2_DECODER_TARGET | MFX_MEMTYPE_FROM_ENCODE,
             surfSrc,
@@ -2078,9 +2326,20 @@
             global.Contains(Glob::AllocRawTmp::Key)
             && !ReleaseResource(Glob::AllocRawTmp::Get(global), task.RawTmp)
             , "task.RawTmp resource is invalid");
+        ThrowAssert(
+            global.Contains(Glob::AllocRawCopy::Key)
+            && !ReleaseResource(Glob::AllocRawCopy::Get(global), task.RawCopy)
+            , "task.RawCopy resource is invalid");
 
-        SetIf(task.pSurfIn, task.pSurfIn && !core.DecreaseReference(*task.pSurfIn), nullptr);
-        ThrowAssert(!!task.pSurfIn, "failed in core.DecreaseReference");
+        if (task.bSurfInRefReleased == false)
+        {
+            SetIf(task.pSurfIn, task.pSurfIn && !core.DecreaseReference(*task.pSurfIn), nullptr);
+            ThrowAssert(!!task.pSurfIn, "failed in core.DecreaseReference");
+        }
+        else
+        {
+            task.pSurfIn = nullptr;
+        }
 
         auto& atrRec = Glob::AllocRec::Get(global);
 
diff --git a/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_legacy.h b/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_legacy.h
--- a/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_legacy.h	2026-04-23 02:53:36.641961132 +0900
+++ b/_studio/mfx_lib/encode_hw/hevc/agnostic/base/hevcehw_base_legacy.h	2026-04-23 04:49:58.353524758 +0900
@@ -249,12 +249,33 @@
             return ::IsSWBRCMode(par);
         }
 
         mfxU16 GetMaxRaw(const mfxVideoParam & par)
         {
             // Extend extra Raw for frames buffered between LA submit and LA Query stage
             const mfxExtCodingOption2* pCO2 = ExtBuffer::Get(par);
             mfxU16 extRaw = pCO2 ? pCO2->LookAheadDepth : 0;
             return par.AsyncDepth + (par.mfx.GopRefDist - 1) + (par.AsyncDepth > 1) + extRaw;
+        }
+        mfxU16 GetMaxRawDecoupled(const mfxVideoParam & par, bool is_high_frame_rate_vpp_di)
+        {
+            mfxU16 rawDepth = GetMaxRaw(par) + 16;
+
+            if (is_high_frame_rate_vpp_di)
+            {
+                rawDepth = mfxU16(rawDepth + 8);
+            }
+
+            return rawDepth;
+        }
+        mfxU16 GetMaxRawCopy(const mfxVideoParam & par, bool is_high_frame_rate_vpp_di)
+        {
+            mfxU16 rawCopyDepth = GetMaxRawDecoupled(par, is_high_frame_rate_vpp_di);
+
+            if (is_high_frame_rate_vpp_di)
+            {
+                rawCopyDepth = mfxU16(rawCopyDepth + 8);
+            }
+
+            return rawCopyDepth;
         }
         mfxU16 GetMaxMBMaps(mfxVideoParam const& par)
         {
diff --git a/_studio/mfx_lib/shared/include/mfx_session.h b/_studio/mfx_lib/shared/include/mfx_session.h
--- a/_studio/mfx_lib/shared/include/mfx_session.h	2026-04-23 02:53:36.672962071 +0900
+++ b/_studio/mfx_lib/shared/include/mfx_session.h	2026-04-23 05:40:45.165643783 +0900
@@ -265,4 +265,3 @@
 mfxStatus MFXInternalPseudoDisjoinSession(mfxSession session);
 
 #endif // _MFX_SESSION_H

diff --git a/_studio/mfx_lib/shared/src/libmfxsw_decode_vp.cpp b/_studio/mfx_lib/shared/src/libmfxsw_decode_vp.cpp
--- a/_studio/mfx_lib/shared/src/libmfxsw_decode_vp.cpp	2026-04-23 02:53:36.672962071 +0900
+++ b/_studio/mfx_lib/shared/src/libmfxsw_decode_vp.cpp	2026-04-23 05:40:45.165643783 +0900
@@ -22,6 +22,7 @@
 #include <mfx_session.h>
 #include <mfx_tools.h>
 #include <mfx_task.h>
+#include <libmfx_allocator.h>
 #include <libmfx_core.h>
 
 #include<set>
@@ -359,6 +360,7 @@
     return MFX_ERR_NONE;
 }
 
+
 class RAIISurfaceArray
 {
 public:
@@ -535,6 +537,9 @@
                 (MFX_ERR_MORE_SURFACE == mfxRes) ||
                 (MFX_WRN_INCOMPATIBLE_VIDEO_PARAM == mfxRes))
             {
+                bool is_vpp_di_output = vpp->IsHardwareDeinterlaceEnabled();
+                SetVppDiOutputFlag(session, vppOut, is_vpp_di_output);
+
                 if (numEntryPoints == 1)
                 {
                     if (mfxRes != MFX_ERR_NONE || !entryPoints[0].pRoutine)
@@ -757,6 +762,7 @@
 
             //delete VPP params, surface pools and ext buffers
             session->m_pDVP.reset();
+            ClearVppDiOutputMemIds(session);
         }
     }
     catch (...)
@@ -786,4 +792,3 @@
 
     return MFX_ERR_NONE;
 }

diff --git a/_studio/mfx_lib/shared/src/libmfxsw_vpp.cpp b/_studio/mfx_lib/shared/src/libmfxsw_vpp.cpp
--- a/_studio/mfx_lib/shared/src/libmfxsw_vpp.cpp	2026-04-23 02:53:36.672962071 +0900
+++ b/_studio/mfx_lib/shared/src/libmfxsw_vpp.cpp	2026-04-23 04:49:58.352524721 +0900
@@ -197,6 +197,15 @@
         {
             MFX_SAFE_CALL(SetupCache(session, *par));
         }
+
+        if (mfxRes >= MFX_ERR_NONE)
+        {
+            SetVppDiScheduler(
+                session,
+                session->m_pScheduler,
+                session->m_pVPP->IsHardwareDeinterlaceEnabled(),
+                session->m_pVPP->IsHighFrameRateDeinterlaceEnabled());
+        }
 #endif // MFX_ENABLE_VPP
     }
     // handle error(s)
@@ -230,6 +239,8 @@
         mfxRes = session->m_pVPP->Close();
 
         session->m_pVPP.reset(nullptr);
+        ClearVppDiOutputMemIds(session);
+        ClearVppDiScheduler(session);
     }
     // handle error(s)
     catch(...)
@@ -314,6 +325,8 @@
             (MFX_ERR_MORE_SURFACE             == mfxRes) ||
             (MFX_WRN_INCOMPATIBLE_VIDEO_PARAM == mfxRes))
         {
+            SetVppDiOutputFlag(session, out, IsVppDiScheduler(session->m_pScheduler));
+
             // prepare the obsolete kind of task.
             // it is obsolete and must be removed.
             if (NULL == entryPoints[0].pRoutine)
diff --git a/_studio/mfx_lib/shared/src/mfx_session.cpp b/_studio/mfx_lib/shared/src/mfx_session.cpp
--- a/_studio/mfx_lib/shared/src/mfx_session.cpp	2026-04-23 02:53:36.673962101 +0900
+++ b/_studio/mfx_lib/shared/src/mfx_session.cpp	2026-04-23 04:33:03.415003880 +0900
@@ -19,12 +19,14 @@
 // SOFTWARE.
 
 #include <assert.h>
+#include <chrono>
 #include <thread>
 #include "mfx_common.h"
 #include <mfx_session.h>
 
 #include <libmfx_core_factory.h>
 #include <libmfx_core.h>
+#include <libmfx_allocator.h>
 
 #include <libmfx_core_vaapi.h>
 #include "mfx_platform_caps.h"
@@ -80,11 +82,12 @@
 
     m_priority = MFX_PRIORITY_NORMAL;
     m_bIsHWENCSupport = false;
-
 } // void _mfxSession::Clear(void)
 
 void _mfxSession::Cleanup(void)
 {
+    const bool is_vpp_di_session = IsVppDiScheduler(m_pScheduler);
+
     // wait until all task are processed
     if (m_pScheduler)
     {
@@ -96,8 +99,69 @@
             m_pScheduler->WaitForAllTasksCompletion(m_pENCODE.get());
     }
 
-    // release the components the excplicit way.
-    // do not relay on default deallocation order,
+    // When the application tears a session down via MFXClose(), component-specific
+    // cleanup would otherwise be deferred entirely to the destructor path.
+    // Explicitly close ENCODE and VPP here so their normal teardown runs before
+    // the component objects are released.
+    if (m_pENCODE.get())
+    {
+        std::ignore = m_pENCODE->Close();
+    }
+
+    if (m_pVPP.get())
+    {
+        std::ignore = m_pVPP->Close();
+        ClearVppDiOutputMemIds(this);
+        ClearVppDiScheduler(this);
+    }
+
+    // Decode+VPP sessions own additional VPP instances under m_pDVP.
+    // MFXClose() previously relied on the destructor path to release them, but
+    // RFF + AFS leaves enough in-flight work behind that the next process can
+    // reopen the stack before those internal VPP pipelines are fully drained.
+    // Mirror the explicit DECODE_VPP close path here so every nested VPP
+    // instance is waited and closed before the session objects are destroyed.
+    if (m_pDVP.get())
+    {
+        if (m_pDECODE.get())
+        {
+            std::ignore = m_pDECODE->Close();
+        }
+
+        for (const auto& pair : m_pDVP->VPPs)
+        {
+            const auto& vpp = pair.second;
+
+            if (m_pScheduler && vpp.get())
+            {
+                std::ignore = m_pScheduler->WaitForAllTasksCompletion(vpp.get());
+            }
+
+            if (vpp.get())
+            {
+                std::ignore = vpp->Close();
+            }
+        }
+
+        m_pDVP.reset();
+        ClearVppDiOutputMemIds(this);
+        ClearVppDiScheduler(this);
+    }
+    else if (m_pDECODE.get())
+    {
+        std::ignore = m_pDECODE->Close();
+    }
+
+    // VEBOX DI sessions are the only path that keeps reproducing cross-process
+    // instability in the matrix run. Give the driver a short drain window after
+    // the explicit Close() path so the next process does not reopen immediately
+    // while the previous DI workload is still unwinding.
+    if (is_vpp_di_session)
+    {
+        std::this_thread::sleep_for(std::chrono::seconds(2));
+    }
+    // Release the components the explicit way.
+    // Do not rely on default deallocation order,
     // somebody could change it.
 
     m_pVPP.reset();
diff --git a/_studio/mfx_lib/vpp/include/mfx_vpp_hw.h b/_studio/mfx_lib/vpp/include/mfx_vpp_hw.h
--- a/_studio/mfx_lib/vpp/include/mfx_vpp_hw.h	2026-04-23 02:53:36.673962101 +0900
+++ b/_studio/mfx_lib/vpp/include/mfx_vpp_hw.h	2026-04-19 23:32:27.555160554 +0900
@@ -913,6 +913,9 @@
             MFX_ENTRY_POINT pEntryPoint[],
             mfxU32 &numEntryPoints);
 
+        bool IsHardwareDeinterlaceEnabled() const;
+        bool IsHighFrameRateDeinterlaceEnabled() const;
+
         mfxStatus RunFrameVPP(mfxFrameSurface1 * /*in*/, mfxFrameSurface1 * /*out*/, mfxExtVppAuxData * /*aux*/)
         {
             //in = in; out = out; aux = aux;
diff --git a/_studio/mfx_lib/vpp/include/mfx_vpp_main.h b/_studio/mfx_lib/vpp/include/mfx_vpp_main.h
--- a/_studio/mfx_lib/vpp/include/mfx_vpp_main.h	2026-04-23 02:53:36.673962101 +0900
+++ b/_studio/mfx_lib/vpp/include/mfx_vpp_main.h	2026-04-19 23:32:27.555160554 +0900
@@ -93,6 +93,8 @@
     }
 
     virtual mfxTaskThreadingPolicy GetThreadingPolicy(void) override;
+    virtual bool IsHardwareDeinterlaceEnabled() const override;
+    virtual bool IsHighFrameRateDeinterlaceEnabled() const override;
 
     // multi threading of SW_VPP functions
     mfxStatus RunVPPTask(
diff --git a/_studio/mfx_lib/vpp/include/mfx_vpp_mvc.h b/_studio/mfx_lib/vpp/include/mfx_vpp_mvc.h
--- a/_studio/mfx_lib/vpp/include/mfx_vpp_mvc.h	2026-04-23 02:53:36.673962101 +0900
+++ b/_studio/mfx_lib/vpp/include/mfx_vpp_mvc.h	2026-04-19 23:32:27.555160554 +0900
@@ -81,6 +81,8 @@
         }
 
         virtual mfxTaskThreadingPolicy GetThreadingPolicy(void) override;
+        virtual bool IsHardwareDeinterlaceEnabled() const override;
+        virtual bool IsHighFrameRateDeinterlaceEnabled() const override;
 
         // multi threading of SW_VPP functions
         mfxStatus RunVPPTask(
diff --git a/_studio/mfx_lib/vpp/include/mfx_vpp_sw.h b/_studio/mfx_lib/vpp/include/mfx_vpp_sw.h
--- a/_studio/mfx_lib/vpp/include/mfx_vpp_sw.h	2026-04-23 02:53:36.673962101 +0900
+++ b/_studio/mfx_lib/vpp/include/mfx_vpp_sw.h	2026-04-19 23:32:27.555160554 +0900
@@ -60,6 +60,8 @@
   virtual mfxStatus GetVPPStat(mfxVPPStat *stat);
   virtual mfxStatus VppFrameCheck(mfxFrameSurface1 *in, mfxFrameSurface1 *out, mfxExtVppAuxData *aux,
                                   MFX_ENTRY_POINT pEntryPoint[], mfxU32 &numEntryPoints);
+  virtual bool IsHardwareDeinterlaceEnabled() const;
+  virtual bool IsHighFrameRateDeinterlaceEnabled() const;
 
   virtual mfxStatus VppFrameCheck(mfxFrameSurface1 *, mfxFrameSurface1 *)
   {
@@ -127,6 +129,8 @@
     virtual mfxStatus Reset(mfxVideoParam *par) override;
 
     virtual mfxStatus GetVideoParam(mfxVideoParam *par) override;
+    virtual bool IsHardwareDeinterlaceEnabled() const override;
+    virtual bool IsHighFrameRateDeinterlaceEnabled() const override;
 
     virtual mfxStatus VppFrameCheck(mfxFrameSurface1 *in, mfxFrameSurface1 *out, mfxExtVppAuxData *aux,
                                     MFX_ENTRY_POINT pEntryPoints[], mfxU32 &numEntryPoints) override;
diff --git a/_studio/mfx_lib/vpp/src/mfx_vpp_hw.cpp b/_studio/mfx_lib/vpp/src/mfx_vpp_hw.cpp
--- a/_studio/mfx_lib/vpp/src/mfx_vpp_hw.cpp	2026-04-23 02:53:36.674962132 +0900
+++ b/_studio/mfx_lib/vpp/src/mfx_vpp_hw.cpp	2026-04-19 23:32:27.555160554 +0900
@@ -3682,6 +3682,17 @@
 
 } // mfxStatus VideoVPPHW::VppFrameCheck(...)
 
+bool VideoVPPHW::IsHardwareDeinterlaceEnabled() const
+{
+    return m_executeParams.iDeinterlacingAlgorithm != 0;
+}
+
+bool VideoVPPHW::IsHighFrameRateDeinterlaceEnabled() const
+{
+    return m_executeParams.iDeinterlacingAlgorithm != 0
+        && m_executeParams.bDeinterlace30i60p;
+}
+
 
 mfxStatus VideoVPPHW::PreWorkOutSurface(ExtSurface & output)
 {
diff --git a/_studio/mfx_lib/vpp/src/mfx_vpp_main.cpp b/_studio/mfx_lib/vpp/src/mfx_vpp_main.cpp
--- a/_studio/mfx_lib/vpp/src/mfx_vpp_main.cpp	2026-04-23 02:53:36.674962132 +0900
+++ b/_studio/mfx_lib/vpp/src/mfx_vpp_main.cpp	2026-04-19 23:32:27.555160554 +0900
@@ -128,6 +128,20 @@
 
 } // mfxTaskThreadingPolicy VideoVPPMain::GetThreadingPolicy(void)
 
+bool VideoVPPMain::IsHardwareDeinterlaceEnabled() const
+{
+    return m_impl.get()
+        ? m_impl->IsHardwareDeinterlaceEnabled()
+        : false;
+}
+
+bool VideoVPPMain::IsHighFrameRateDeinterlaceEnabled() const
+{
+    return m_impl.get()
+        ? m_impl->IsHighFrameRateDeinterlaceEnabled()
+        : false;
+}
+
 
 mfxStatus VideoVPPMain::VppFrameCheck(mfxFrameSurface1 *in,
                                       mfxFrameSurface1 *out,
diff --git a/_studio/mfx_lib/vpp/src/mfx_vpp_mvc.cpp b/_studio/mfx_lib/vpp/src/mfx_vpp_mvc.cpp
--- a/_studio/mfx_lib/vpp/src/mfx_vpp_mvc.cpp	2026-04-23 02:53:36.674962132 +0900
+++ b/_studio/mfx_lib/vpp/src/mfx_vpp_mvc.cpp	2026-04-19 23:32:27.555160554 +0900
@@ -234,6 +234,22 @@
 
 } // mfxTaskThreadingPolicy ImplementationMvc::GetThreadingPolicy(void)
 
+bool ImplementationMvc::IsHardwareDeinterlaceEnabled() const
+{
+    return std::any_of(m_VPP.begin(), m_VPP.end(), [](const auto& vpp_entry)
+    {
+        return vpp_entry.second != nullptr && vpp_entry.second->IsHardwareDeinterlaceEnabled();
+    });
+}
+
+bool ImplementationMvc::IsHighFrameRateDeinterlaceEnabled() const
+{
+    return std::any_of(m_VPP.begin(), m_VPP.end(), [](const auto& vpp_entry)
+    {
+        return vpp_entry.second != nullptr && vpp_entry.second->IsHighFrameRateDeinterlaceEnabled();
+    });
+}
+
 
 mfxStatus ImplementationMvc::VppFrameCheck(
     mfxFrameSurface1 *in,
diff --git a/_studio/mfx_lib/vpp/src/mfx_vpp_sw_core.cpp b/_studio/mfx_lib/vpp/src/mfx_vpp_sw_core.cpp
--- a/_studio/mfx_lib/vpp/src/mfx_vpp_sw_core.cpp	2026-04-23 02:53:36.674962132 +0900
+++ b/_studio/mfx_lib/vpp/src/mfx_vpp_sw_core.cpp	2026-04-23 05:40:45.166643809 +0900
@@ -276,6 +276,16 @@
     return sts;
 } // mfxStatus VideoVPPBase::VppFrameCheck(...)
 
+bool VideoVPPBase::IsHardwareDeinterlaceEnabled() const
+{
+    return false;
+}
+
+bool VideoVPPBase::IsHighFrameRateDeinterlaceEnabled() const
+{
+    return false;
+}
+
 
 mfxStatus VideoVPPBase::QueryIOSurf(VideoCORE* core, mfxVideoParam *par, mfxFrameAllocRequest *request)
 {
@@ -1388,6 +1398,20 @@
     return sts;
 }
 
+bool VideoVPP_HW::IsHardwareDeinterlaceEnabled() const
+{
+    return m_pHWVPP.get()
+        ? m_pHWVPP->IsHardwareDeinterlaceEnabled()
+        : false;
+}
+
+bool VideoVPP_HW::IsHighFrameRateDeinterlaceEnabled() const
+{
+    return m_pHWVPP.get()
+        ? m_pHWVPP->IsHighFrameRateDeinterlaceEnabled()
+        : false;
+}
+
 mfxStatus VideoVPP_HW::VppFrameCheck(mfxFrameSurface1 *in, mfxFrameSurface1 *out, mfxExtVppAuxData *aux,
                                 MFX_ENTRY_POINT pEntryPoints[], mfxU32 &numEntryPoints)
 {
@@ -1456,4 +1480,3 @@
 
 #endif // MFX_ENABLE_VPP
 /* EOF */

diff --git a/_studio/shared/include/libmfx_allocator.h b/_studio/shared/include/libmfx_allocator.h
--- a/_studio/shared/include/libmfx_allocator.h	2026-04-23 02:53:36.677962223 +0900
+++ b/_studio/shared/include/libmfx_allocator.h	2026-04-23 04:49:58.352524721 +0900
@@ -36,6 +36,8 @@
 #include <mutex>
 #include <condition_variable>
 #include <algorithm>
+#include <unordered_map>
+#include <unordered_set>
 
 #ifdef MFX_ENABLE_ENCODE_STATS
 #include "mfxencodestats.h"
@@ -44,6 +46,266 @@
 static const size_t BASE_ADDR_ALIGN = 0x1000; // 4k page size alignment
 static const size_t BASE_SIZE_ALIGN = 0x1000; // 4k page size alignment
 
+struct VppDiMemIdRegistryState
+{
+    std::mutex                                             mutex;
+    std::unordered_map<mfxSession, std::unordered_set<mfxMemId>> session_to_memids;
+    std::unordered_map<mfxMemId, uint32_t>                 memid_refcounts;
+    std::unordered_map<mfxSession, bool>                   session_is_high_frame_rate_di;
+    std::unordered_map<mfxSession, MFXIScheduler *>        session_to_scheduler;
+    std::unordered_map<MFXIScheduler *, uint32_t>          scheduler_refcounts;
+    std::unordered_map<MFXIScheduler *, uint32_t>          scheduler_high_frame_rate_refcounts;
+};
+
+inline VppDiMemIdRegistryState& GetVppDiMemIdRegistryState()
+{
+    static VppDiMemIdRegistryState state;
+    return state;
+}
+
+inline void SetVppDiOutputMemId(mfxSession session, mfxMemId mid, bool is_vpp_di_output)
+{
+    if (session == nullptr || mid == nullptr)
+    {
+        return;
+    }
+
+    auto& state = GetVppDiMemIdRegistryState();
+    std::lock_guard<std::mutex> lock(state.mutex);
+    auto& session_memids = state.session_to_memids[session];
+
+    if (is_vpp_di_output)
+    {
+        const auto insert_result = session_memids.insert(mid);
+        if (insert_result.second)
+        {
+            state.memid_refcounts[mid] += 1;
+        }
+    }
+    else
+    {
+        const size_t erased_count = session_memids.erase(mid);
+        if (erased_count == 0)
+        {
+            return;
+        }
+
+        auto refcount = state.memid_refcounts.find(mid);
+        if (refcount == std::end(state.memid_refcounts))
+        {
+            return;
+        }
+
+        if (refcount->second > 1)
+        {
+            refcount->second -= 1;
+        }
+        else
+        {
+            state.memid_refcounts.erase(refcount);
+        }
+
+        if (session_memids.empty())
+        {
+            state.session_to_memids.erase(session);
+        }
+    }
+}
+
+inline bool IsVppDiOutputMemId(mfxMemId mid)
+{
+    if (mid == nullptr)
+    {
+        return false;
+    }
+
+    auto& state = GetVppDiMemIdRegistryState();
+    std::lock_guard<std::mutex> lock(state.mutex);
+    return state.memid_refcounts.find(mid) != std::end(state.memid_refcounts);
+}
+
+inline void SetVppDiScheduler(
+    mfxSession session,
+    MFXIScheduler *scheduler,
+    bool is_vpp_di_session,
+    bool is_high_frame_rate_di_session = false)
+{
+    if (session == nullptr || scheduler == nullptr)
+    {
+        return;
+    }
+
+    auto &state = GetVppDiMemIdRegistryState();
+    std::lock_guard<std::mutex> lock(state.mutex);
+
+    auto existing_scheduler = state.session_to_scheduler.find(session);
+    if (existing_scheduler != std::end(state.session_to_scheduler))
+    {
+        auto refcount = state.scheduler_refcounts.find(existing_scheduler->second);
+        if (refcount != std::end(state.scheduler_refcounts))
+        {
+            if (refcount->second > 1)
+            {
+                refcount->second -= 1;
+            }
+            else
+            {
+                state.scheduler_refcounts.erase(refcount);
+            }
+        }
+
+        auto existing_high_frame_rate = state.session_is_high_frame_rate_di.find(session);
+        if (existing_high_frame_rate != std::end(state.session_is_high_frame_rate_di)
+            && existing_high_frame_rate->second)
+        {
+            auto high_frame_rate_refcount = state.scheduler_high_frame_rate_refcounts.find(existing_scheduler->second);
+            if (high_frame_rate_refcount != std::end(state.scheduler_high_frame_rate_refcounts))
+            {
+                if (high_frame_rate_refcount->second > 1)
+                {
+                    high_frame_rate_refcount->second -= 1;
+                }
+                else
+                {
+                    state.scheduler_high_frame_rate_refcounts.erase(high_frame_rate_refcount);
+                }
+            }
+        }
+
+        state.session_to_scheduler.erase(existing_scheduler);
+        state.session_is_high_frame_rate_di.erase(session);
+    }
+
+    if (is_vpp_di_session == false)
+    {
+        return;
+    }
+
+    state.session_to_scheduler[session] = scheduler;
+    state.scheduler_refcounts[scheduler] += 1;
+    state.session_is_high_frame_rate_di[session] = is_high_frame_rate_di_session;
+
+    if (is_high_frame_rate_di_session)
+    {
+        state.scheduler_high_frame_rate_refcounts[scheduler] += 1;
+    }
+}
+
+inline bool IsVppDiScheduler(MFXIScheduler *scheduler)
+{
+    if (scheduler == nullptr)
+    {
+        return false;
+    }
+
+    auto &state = GetVppDiMemIdRegistryState();
+    std::lock_guard<std::mutex> lock(state.mutex);
+
+    return state.scheduler_refcounts.find(scheduler) != std::end(state.scheduler_refcounts);
+}
+
+inline bool IsHighFrameRateVppDiScheduler(MFXIScheduler *scheduler)
+{
+    if (scheduler == nullptr)
+    {
+        return false;
+    }
+
+    auto &state = GetVppDiMemIdRegistryState();
+    std::lock_guard<std::mutex> lock(state.mutex);
+
+    return state.scheduler_high_frame_rate_refcounts.find(scheduler)
+        != std::end(state.scheduler_high_frame_rate_refcounts);
+}
+
+inline void ClearVppDiScheduler(mfxSession session)
+{
+    if (session == nullptr)
+    {
+        return;
+    }
+
+    auto &state = GetVppDiMemIdRegistryState();
+    std::lock_guard<std::mutex> lock(state.mutex);
+
+    auto existing_scheduler = state.session_to_scheduler.find(session);
+    if (existing_scheduler == std::end(state.session_to_scheduler))
+    {
+        return;
+    }
+
+    auto refcount = state.scheduler_refcounts.find(existing_scheduler->second);
+    if (refcount != std::end(state.scheduler_refcounts))
+    {
+        if (refcount->second > 1)
+        {
+            refcount->second -= 1;
+        }
+        else
+        {
+            state.scheduler_refcounts.erase(refcount);
+        }
+    }
+
+    auto existing_high_frame_rate = state.session_is_high_frame_rate_di.find(session);
+    if (existing_high_frame_rate != std::end(state.session_is_high_frame_rate_di)
+        && existing_high_frame_rate->second)
+    {
+        auto high_frame_rate_refcount = state.scheduler_high_frame_rate_refcounts.find(existing_scheduler->second);
+        if (high_frame_rate_refcount != std::end(state.scheduler_high_frame_rate_refcounts))
+        {
+            if (high_frame_rate_refcount->second > 1)
+            {
+                high_frame_rate_refcount->second -= 1;
+            }
+            else
+            {
+                state.scheduler_high_frame_rate_refcounts.erase(high_frame_rate_refcount);
+            }
+        }
+    }
+
+    state.session_is_high_frame_rate_di.erase(session);
+    state.session_to_scheduler.erase(existing_scheduler);
+}
+
+inline void ClearVppDiOutputMemIds(mfxSession session)
+{
+    if (session == nullptr)
+    {
+        return;
+    }
+
+    auto& state = GetVppDiMemIdRegistryState();
+    std::lock_guard<std::mutex> lock(state.mutex);
+
+    auto session_memids = state.session_to_memids.find(session);
+    if (session_memids == std::end(state.session_to_memids))
+    {
+        return;
+    }
+
+    for (auto mid : session_memids->second)
+    {
+        auto refcount = state.memid_refcounts.find(mid);
+        if (refcount == std::end(state.memid_refcounts))
+        {
+            continue;
+        }
+
+        if (refcount->second > 1)
+        {
+            refcount->second -= 1;
+        }
+        else
+        {
+            state.memid_refcounts.erase(refcount);
+        }
+    }
+
+    state.session_to_memids.erase(session_memids);
+}
+
 // Internal Allocators
 namespace mfxDefaultAllocator
 {
@@ -828,6 +1090,16 @@
         m_sp = Sync;
     }
 
+    void SetVppDiOutput(bool is_vpp_di_output)
+    {
+        m_is_vpp_di_output = is_vpp_di_output;
+    }
+
+    bool IsVppDiOutput() const
+    {
+        return m_is_vpp_di_output;
+    }
+
     void DetachParentAllocator()
     {
         m_allocator = nullptr;
@@ -862,6 +1134,7 @@
     mfxMemId                 m_mid;
     mfxSyncPoint             m_sp          = nullptr;
     mfxSurfacePoolInterface* m_parent_pool = nullptr;
+    bool                     m_is_vpp_di_output = false;
 };
 
 template <>
@@ -883,6 +1156,21 @@
 
 class mfxFrameSurfaceInterfaceImpl;
 
+inline void SetVppDiOutputFlag(mfxSession session, mfxFrameSurface1 *surface, bool is_vpp_di_output)
+{
+    if (surface == nullptr)
+    {
+        return;
+    }
+
+    SetVppDiOutputMemId(session, surface->Data.MemId, is_vpp_di_output);
+
+    if (surface->FrameInterface != nullptr && surface->FrameInterface->Context != nullptr)
+    {
+        static_cast<mfxFrameSurfaceBaseInterface *>(surface->FrameInterface->Context)->SetVppDiOutput(is_vpp_di_output);
+    }
+}
+
 class mfxSurfaceBase
     : public mfxRefCountableImpl<mfxSurfaceInterface>
 {
@@ -1503,4 +1791,3 @@
 };
 
 #endif

diff --git a/_studio/shared/include/mfxvideo++int.h b/_studio/shared/include/mfxvideo++int.h
--- a/_studio/shared/include/mfxvideo++int.h	2026-04-23 02:53:36.678962253 +0900
+++ b/_studio/shared/include/mfxvideo++int.h	2026-04-19 23:32:27.555160554 +0900
@@ -570,6 +570,18 @@
     virtual
     mfxStatus RunFrameVPP(mfxFrameSurface1 *in, mfxFrameSurface1 *out, mfxExtVppAuxData *aux) = 0;
 
+    virtual
+    bool IsHardwareDeinterlaceEnabled() const
+    {
+        return false;
+    }
+
+    virtual
+    bool IsHighFrameRateDeinterlaceEnabled() const
+    {
+        return false;
+    }
+
     mfxStatus ResetCache(mfxVideoParam* par)
     {
         MFX_CHECK_NULL_PTR1(par);
