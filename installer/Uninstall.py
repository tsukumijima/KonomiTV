
from rich import print
from rich.padding import Padding
from rich.text import Text


def Uninstall() -> None:
    """
    KonomiTV をアンインストールする (アンインストーラーの実装)
    """

    print(Padding(Text(
        'アンインストーラーはまだ実装されていません。'
    ), (1, 2, 0, 2)))
